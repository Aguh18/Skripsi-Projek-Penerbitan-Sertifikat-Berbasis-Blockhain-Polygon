{
  "name": "certificate-registry",
  "version": "1.0.0",
  "description": "Certificate Registry Smart Contract",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "npx hardhat run scripts/deploy-amoy.js --network amoy",
    "deploy:polygon": "npx hardhat run scripts/deploy-polygon.js --network polygon",
    "verify:polygon": "npx hardhat run scripts/verify-contract.js --network polygon",
    "node": "npx hardhat node",
    "clean": "npx hardhat clean"
  },
  "keywords": [
    "blockchain",
    "polygon",
    "certificate",
    "smart-contract"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@nomicfoundation/hardhat-verify": "^2.0.14",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "chai": "^4.3.7",
    "dotenv": "^16.0.0",
    "ethereum-waffle": "^4.0.10",
    "ethers": "^5.7.2",
    "hardhat": "^2.12.0"
  }
}
