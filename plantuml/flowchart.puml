@startuml
start
:User membuka aplikasi;
:Login;
if (Login sukses?) then (ya)
  if (Role = Admin?) then (ya)
    :Lihat permintaan issuer;
    if (Approve issuer?) then (ya)
      :Tambah issuer ke blockchain;
      :Update status user di DB;
    else (tidak)
    endif
    :Lihat semua sertifikat;
  else (tidak)
    if (Role = Issuer?) then (ya)
      :Upload sertifikat (single/bulk);
      :Simpan sebagai draft di DB;
      :Pilih draft untuk diterbitkan;
      :Publish ke blockchain (bulk/single);
      :Update status sertifikat di DB;
    else (tidak)
      :Lihat/Verifikasi sertifikat;
    endif
  endif
else (tidak)
  :Tampilkan error login;
endif
stop
@enduml 